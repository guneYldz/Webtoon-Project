export const metadata = {
    title: "Keşfet | Kaos Manga",
    alternates: { canonical: "https://kaosmanga.net/kesfet" },
};

export default function KesfetLayout({ children }) {
    return <>{children}</>;
}
